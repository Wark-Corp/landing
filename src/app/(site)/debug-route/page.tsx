export const dynamic = 'force-dynamic';

export default function DebugPage() {
    return (
        <div style={{ padding: '100px', background: 'white', color: 'black' }}>
            <h1>DEBUG PAGE</h1>
            <p>If you see this, routing works for new paths.</p>
        </div>
    );
}
